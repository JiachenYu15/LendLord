<%= link_to "Home", root_path%>&nbsp&nbsp
<%= link_to "About Me", home_about_path%>&nbsp&nbsp
<%= link_to "Personal Home", controller: 'personal_home'%>&nbsp&nbsp
<%= link_to 'Transaction', controller: 'transactions' %>&nbsp&nbsp
<%= link_to 'Items', controller: 'items' %>&nbsp&nbsp
<%= link_to "Friends", friends_path%>&nbsp&nbsp
&nbsp&nbsp&nbsp&nbsp <%= link_to "Log Out", logout_path, method: :delete%>

<h1>This is Manage Page</h1>
This page is now displaying all the friend requests that the user has received<br><br>

<% unless @myfriends.blank? %>
<table border="1">
  <tr>
    <th>Requester</th>
    <th>Requestee</th>
    <th>Action</th>
  </tr>
  <% @myfriends.each do |entry| %>
    <tr>
      <td><%= entry.user_from_id %></td>
      <td><%= entry.user_to_id %></td>
      <td><%= link_to "Accept", accept_friend_path(entry.id) %>
          <%= link_to "Reject", reject_friend_path(entry.id) %>    
      </td>
    </tr>
  <% end %>
</table><br>
<% else %>
There are no pending friend requests sent to you.
<% end %>
